@model List<Product>

<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2>
                Products List
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Update" class="btn btn-primary">
               <i class="bi bi-plus-circle"></i> Add new product
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped" id="myTable" style="width:100%"> 
    <thead>
        <tr>        
            <th>Title</th>
            <th>Author</th>
            <th>ISBN</th>
            <th>Price</th>
            <th>Category</th>
            <th>Actions</th>
            @* <th>Edit</th>
            <th>Delete</th> *@
        </tr>
    </thead>
    <tbody>
        @* @foreach(var product in Model)
        {
            <tr>
                <td>@product.Title</td>
                <td>@product.Author</td>
                <td>@product.ISBN</td>
                <td>@product.Price</td>
                <td>@product.Category.Name</td>
                <td>
                    <div class="w-75">
                        <a asp-controller="Product" asp-action="Update" asp-route-id="@product.Id" class="btn btn-primary mx-2">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                    </div>
                </td>
                <td>
                    <div class="w-75">
                        <a asp-controller="Product" asp-action="Delete" asp-route-id="@product.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash-fill"></i>
                        </a>
                    </div>
                </td>
            </tr>
        }*@
    </tbody>
</table>

@section Scripts{
    <script src="~/js/product.js"></script>
}